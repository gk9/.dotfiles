# TAR Backup / Restore
alias backup_home="tar -cvpzf $HOME/home_backup.tar.gz --exclude=$HOME/home_backup.tar.gz --exclude=$HOME/.dbus --exclude=$HOME/.cache $HOME"
alias restore_home="tar -xvpzf ./home_backup.tar.gz -C / --numeric-owner"
